# Installed FPF profile (local dev instance, not the scaffold source)
/CLAUDE.md
/.claude/
/.fpf/

# Dev-only scripts
/sync.sh

# Install artifacts (from running installer in the dev repo)
/tweakcc-prompts/
/.level0-versions
